# This script must be run from the pyaf directory to setup the path to the pyaf modules.
# the goal is only to add the 'pyaf' parent directory in the PYTHONPATH envioronemnt variable.
export PYTHONPATH=$PYTHONPATH:`pwd`/..


